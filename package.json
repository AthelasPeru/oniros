{
  "name": "Oniros",
  "version": "0.6.0",
  "description": "Athelas Wordpress Boilerplate",
  "main": "gulpfile.js",
  "scripts": {
    "main:scss": "mkdir -p dist/css/main && node-sass --output-style nested --indent-type tab --indent-width 1 --source-map true -o dist/css/main source/scss/main/main.scss",
    "main:scss:min": "mkdir -p dist/css/main && node-sass --output-style compressed source/scss/main/main.scss > dist/css/main/main.min.css",
    "main:autoprefixer": "postcss -u autoprefixer -r dist/css/main/*.css",
    "main:build:css": "npm run main:scss && npm run main:autoprefixer",

    "admin:scss": "mkdir -p dist/css/admin && node-sass --output-style nested --indent-type tab --indent-width 1 --source-map true -o dist/css/admin source/scss/admin/admin.scss",
    "admin:scss:min": "mkdir -p dist/css/admin && node-sass --output-style compressed source/scss/admin/admin.scss > dist/css/admin/admin.min.css",
    "admin:autoprefixer": "postcss -u autoprefixer -r dist/css/admin/*.css",
    "admin:build:css": "npm run admin:scss && npm run admin:autoprefixer",

    "login:scss": "mkdir -p dist/css/login && node-sass --output-style nested --indent-type tab --indent-width 1 --source-map true -o dist/css/login source/scss/login/login.scss",
    "login:scss:min": "mkdir -p dist/css/login && node-sass --output-style compressed source/scss/login/login.scss > dist/css/login/login.min.css",
    "login:autoprefixer": "postcss -u autoprefixer -r dist/css/login/*.css",
    "login:build:css": "npm run login:scss && npm run login:autoprefixer",

    "all:build:css": "parallelshell 'npm run main:build:css' 'npm run admin:build:css' 'npm run login:build:css' "

  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "author": "Athelas Peru",
  "license": "Creative Commons",
  "devDependencies": {
    "autoprefixer": "^6.3.3",
    "browser-sync": "^2.11.1",
    "imagemin-cli": "^2.1.0",
    "node-sass": "^3.4.2",
    "onchange": "^2.0.0",
    "parallelshell": "^2.0.0",
    "postcss-cli": "^2.5.1",
    "svg-sprite-generator": "0.0.7",
    "svgo": "^0.6.1",
    "uglify-js": "^2.6.1"
  },
  "homepage": "http://www.athelas.pe/oniros",
  "dependencies": {}
}
